<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INEMA FTD - Assistente Virtual</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        deepseek: {
                            light: '#1a8cff',
                            DEFAULT: '#0066cc',
                            dark: '#0052a3',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .message {
            animation: fadeIn 0.3s ease-out forwards;
        }
        
        .typing-indicator {
            display: inline-block;
        }
        
        .typing-indicator span {
            display: inline-block;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background-color: #0066cc;
            margin: 0 2px;
            opacity: 0.4;
        }
        
        .typing-indicator span:nth-child(1) {
            animation: bounce 1s infinite;
        }
        
        .typing-indicator span:nth-child(2) {
            animation: bounce 1s infinite 0.2s;
        }
        
        .typing-indicator span:nth-child(3) {
            animation: bounce 1s infinite 0.4s;
        }
        
        @keyframes bounce {
            0%, 100% { transform: translateY(0); opacity: 0.4; }
            50% { transform: translateY(-5px); opacity: 1; }
        }
        
        .chat-container {
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            transition: all 0.3s ease;
            resize: both;
            overflow: hidden;
            min-width: 400px;
            min-height: 600px;
            max-width: 800px;
            max-height: 800px;
        }
        
        .chat-header {
            background: linear-gradient(135deg, #1a8cff 0%, #0066cc 100%);
        }
        
        .user-message {
            background-color: #0066cc;
            color: white;
            border-radius: 18px 18px 4px 18px;
        }
        
        .bot-message {
            background-color: #f0f8ff;
            color: #003366;
            border-radius: 18px 18px 18px 4px;
        }
        
        .send-btn {
            transition: all 0.2s;
            background-color: #0066cc;
        }
        
        .send-btn:hover {
            transform: scale(1.1);
            background-color: #0052a3;
        }
        
        .send-btn:active {
            transform: scale(0.95);
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        .pulse {
            animation: pulse 1.5s infinite;
        }
        
        .login-container {
            background: linear-gradient(135deg, #f0f8ff 0%, #e1f0ff 100%);
        }
        
        .login-btn {
            background: linear-gradient(135deg, #1a8cff 0%, #0066cc 100%);
            transition: all 0.3s;
        }
        
        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        
        .resize-handle {
            position: absolute;
            bottom: 0;
            right: 0;
            width: 20px;
            height: 20px;
            cursor: nwse-resize;
            background-color: #0066cc;
            border-top-left-radius: 4px;
        }
    </style>
</head>
<body class="bg-blue-50 min-h-screen flex items-center justify-center p-4">
    <!-- Login Screen -->
    <div id="login-screen" class="login-container rounded-2xl p-8 max-w-md w-full shadow-lg">
        <div class="text-center mb-8">
            <div class="w-16 h-16 rounded-full bg-deepseek mx-auto flex items-center justify-center mb-4">
                <i class="fas fa-robot text-white text-2xl"></i>
            </div>
            <h1 class="text-2xl font-bold text-deepseek-dark">INEMA FTD</h1>
            <p class="text-deepseek">Projeto de Transformação Digital</p>
        </div>
        
        <div class="space-y-4">
            <div>
                <label for="username" class="block text-sm font-medium text-deepseek-dark mb-1">Usuário</label>
                <input 
                    type="text" 
                    id="username" 
                    placeholder="Digite seu usuário" 
                    class="w-full px-4 py-2 rounded-lg border border-deepseek-light focus:outline-none focus:ring-2 focus:ring-deepseek focus:border-transparent"
                >
            </div>
            
            <div>
                <label for="password" class="block text-sm font-medium text-deepseek-dark mb-1">Senha</label>
                <input 
                    type="password" 
                    id="password" 
                    placeholder="Digite sua senha" 
                    class="w-full px-4 py-2 rounded-lg border border-deepseek-light focus:outline-none focus:ring-2 focus:ring-deepseek focus:border-transparent"
                >
            </div>
            
            <button 
                id="login-btn" 
                class="login-btn w-full py-2 rounded-lg text-white font-medium"
            >
                Entrar
            </button>
        </div>
    </div>
    
    <!-- Chat Container (initially hidden) -->
    <div id="chat-container" class="chat-container bg-white rounded-2xl h-[600px] flex flex-col overflow-hidden hidden relative">
        <div class="resize-handle"></div>
        
        <!-- Header -->
        <div class="chat-header p-4 text-white flex items-center">
            <div class="w-10 h-10 rounded-full bg-white/20 flex items-center justify-center mr-3">
                <i class="fas fa-robot text-xl"></i>
            </div>
            <div>
                <h1 class="font-bold text-lg">INEMA FTD</h1>
                <p class="text-xs opacity-80">Assistente Virtual de Transformação Digital</p>
            </div>
            <button id="clear-chat" class="ml-auto text-white/70 hover:text-white" title="Limpar conversa">
                <i class="fas fa-trash-alt"></i>
            </button>
        </div>
        
        <!-- Chat messages -->
        <div id="chat-messages" class="flex-1 p-4 overflow-y-auto space-y-3">
            <!-- Messages will be added here dynamically -->
        </div>
        
        <!-- Input area -->
        <div class="p-3 border-t border-deepseek-light/20 bg-blue-50">
            <div class="flex items-center">
                <input 
                    id="user-input" 
                    type="text" 
                    placeholder="Digite sua mensagem..." 
                    class="flex-1 border border-deepseek-light rounded-full py-2 px-4 focus:outline-none focus:ring-2 focus:ring-deepseek focus:border-transparent"
                    autocomplete="off"
                >
                <button 
                    id="send-btn" 
                    class="send-btn text-white w-10 h-10 rounded-full ml-2 flex items-center justify-center"
                    title="Enviar mensagem"
                >
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
            <p class="text-xs text-deepseek mt-2 text-center">
                Assistente virtual do Projeto de Transformação Digital - INEMA
            </p>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const loginScreen = document.getElementById('login-screen');
            const chatContainer = document.getElementById('chat-container');
            const chatMessages = document.getElementById('chat-messages');
            const userInput = document.getElementById('user-input');
            const sendBtn = document.getElementById('send-btn');
            const clearChatBtn = document.getElementById('clear-chat');
            const loginBtn = document.getElementById('login-btn');
            const usernameInput = document.getElementById('username');
            const passwordInput = document.getElementById('password');
            
            let currentUser = null;
            
            // Updated Webhook URL
            const WEBHOOK_URL = 'https://webhk.inema.online/webhook/32b289bc-d4a7-419c-a6a3-74572502c752';
            
            // Add message to chat
            function addMessage(content, isUser = false) {
                const messageDiv = document.createElement('div');
                messageDiv.className = `message ${isUser ? 'user-message ml-auto' : 'bot-message'} p-3 max-w-[80%]`;
                
                // Convert newlines to <br> tags
                const formattedContent = content.replace(/\n/g, '<br>');
                messageDiv.innerHTML = `<p>${formattedContent}</p>`;
                
                chatMessages.appendChild(messageDiv);
                chatMessages.scrollTop = chatMessages.scrollHeight;
                
                // Add pulse animation to new messages
                messageDiv.classList.add('pulse');
                setTimeout(() => {
                    messageDiv.classList.remove('pulse');
                }, 1500);
            }
            
            // Show typing indicator
            function showTypingIndicator() {
                const typingDiv = document.createElement('div');
                typingDiv.className = 'bot-message message p-3 max-w-[80%]';
                typingDiv.id = 'typing-indicator';
                typingDiv.innerHTML = `
                    <div class="typing-indicator flex space-x-1">
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                `;
                chatMessages.appendChild(typingDiv);
                chatMessages.scrollTop = chatMessages.scrollHeight;
            }
            
            // Hide typing indicator
            function hideTypingIndicator() {
                const typingIndicator = document.getElementById('typing-indicator');
                if (typingIndicator) {
                    typingIndicator.remove();
                }
            }
            
            // Send message to webhook with user identification
            async function sendMessageToWebhook(message) {
                try {
                    showTypingIndicator();
                    
                    const response = await fetch(WEBHOOK_URL, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({
                            message: message,
                            user: currentUser // Send user identification to webhook
                        })
                    });
                    
                    const responseText = await response.text();
                    hideTypingIndicator();
                    
                    if (responseText) {
                        addMessage(responseText, false);
                    } else {
                        addMessage("Não consegui processar sua solicitação. Por favor, tente novamente.", false);
                    }
                } catch (error) {
                    hideTypingIndicator();
                    addMessage("Ocorreu um erro ao conectar com o servidor. Por favor, tente novamente mais tarde.", false);
                    console.error('Error:', error);
                }
            }
            
            // Handle send message
            function handleSendMessage() {
                const message = userInput.value.trim();
                if (message) {
                    addMessage(message, true);
                    userInput.value = '';
                    sendMessageToWebhook(message);
                }
            }
            
            // Handle login
            loginBtn.addEventListener('click', function() {
                const username = usernameInput.value.trim();
                const password = passwordInput.value.trim();
                
                if (username) {
                    currentUser = username; // Store the username
                    
                    // Hide login screen and show chat
                    loginScreen.classList.add('hidden');
                    chatContainer.classList.remove('hidden');
                    
                    // Add welcome message
                    addMessage(`Olá, ${username}! Sou o assistente virtual do INEMA FTD - Projeto de Transformação Digital. Como posso te ajudar hoje?`, false);
                    
                    // Focus input
                    userInput.focus();
                } else {
                    alert('Por favor, informe um nome de usuário.');
                }
            });
            
            // Also allow login with Enter key
            passwordInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    loginBtn.click();
                }
            });
            
            // Event listeners for chat
            sendBtn.addEventListener('click', handleSendMessage);
            
            userInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    handleSendMessage();
                }
            });
            
            // Clear chat
            clearChatBtn.addEventListener('click', function() {
                chatMessages.innerHTML = '';
                addMessage(`Olá novamente, ${currentUser}! Como posso te ajudar hoje?`, false);
            });
            
            // Resize functionality
            const resizeHandle = document.querySelector('.resize-handle');
            const chatBox = document.querySelector('.chat-container');
            
            resizeHandle.addEventListener('mousedown', function(e) {
                e.preventDefault();
                
                const startX = e.clientX;
                const startY = e.clientY;
                const startWidth = parseInt(document.defaultView.getComputedStyle(chatBox).width, 10);
                const startHeight = parseInt(document.defaultView.getComputedStyle(chatBox).height, 10);
                
                function doDrag(e) {
                    chatBox.style.width = (startWidth + e.clientX - startX) + 'px';
                    chatBox.style.height = (startHeight + e.clientY - startY) + 'px';
                }
                
                function stopDrag() {
                    document.documentElement.removeEventListener('mousemove', doDrag, false);
                    document.documentElement.removeEventListener('mouseup', stopDrag, false);
                }
                
                document.documentElement.addEventListener('mousemove', doDrag, false);
                document.documentElement.addEventListener('mouseup', stopDrag, false);
            });
        });
    </script>
</body>
</html>